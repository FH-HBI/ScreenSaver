using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartSpeller : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Application.OpenURL("C:/Users/Felix/Desktop/Neues Textdokument (2).txt");
        Screen.SetResolution(Screen.width, Screen.height / 2, false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
